Queue tile for the play dashboard; set primary on h2h_rapid / ffa4_standard.

```jsx
<QueueCard name="FFA-4 Standard" tagline="Four players, eight rounds" players={4} rounds={8} minutes={55} pool="OpenSkill" primary onPlay={fn} />
```
