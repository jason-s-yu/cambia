Square icon-only button matching Button metrics; children is the icon node (Lucide).

```jsx
<IconButton title="Settings"><i data-lucide="sliders-horizontal"></i></IconButton>
```

Variants: secondary (default, outlined piece), ghost.
