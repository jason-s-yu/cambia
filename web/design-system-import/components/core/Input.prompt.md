Text input with uppercase eyebrow label, inset surface, honey focus ring.

```jsx
<Input label="Lobby name" placeholder="Friday night circuit" />
<Input label="Turn timer (sec)" mono defaultValue="30" error="Must be ≥ 0" />
```
